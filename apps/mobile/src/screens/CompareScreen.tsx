// Compare — same prompt, 2–4 models side by side, savings banner.
//
// TODO(implementation):
//  - Model multi-select chips (max 4)
//  - engine.compareModels → ranked cards with GradeBadge + metrics
//  - "Switching saves X%" banner phrased with relatable units

import React from 'react';
import { ScrollView, Text } from 'react-native';

export default function CompareScreen() {
  return (
    <ScrollView>
      <Text>Compare — to be implemented</Text>
    </ScrollView>
  );
}
