// Settings — default model, electricity region (carbon), reasoning effort,
// embodied-carbon toggle. Persisted via storage.getSettings/setSettings.

import React from 'react';
import { View, Text } from 'react-native';

export default function SettingsScreen() {
  return (
    <View>
      <Text>Settings — to be implemented</Text>
    </View>
  );
}
